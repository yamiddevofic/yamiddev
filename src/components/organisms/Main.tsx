import React from 'react';
import Section from '../molecules/Section';

const Main = () => (
  <div>
    <Section title="Bienvenido" content="Esta es la sección principal." />
    <Section title="Acerca de" content="Información sobre nosotros." />
  </div>
);

export default Main;
